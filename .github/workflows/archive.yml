name: YouTube Archive HYBRID

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

concurrency:
  group: youtube-archive
  cancel-in-progress: false

jobs:
  download-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -U "yt-dlp @ https://github.com/yt-dlp/yt-dlp/archive/master.tar.gz"
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash

      - name: Setup rclone config
        run: |
          mkdir -p ~/.config/rclone
          echo "${{ secrets.RCLONE_CONFIG }}" | base64 --decode > ~/.config/rclone/rclone.conf

      # 🔐 COOKIES
      - name: Write cookies
        run: |
          echo "${{ secrets.YOUTUBE_COOKIES }}" > cookies.txt

      # 🔥 HISTORIAL
      - name: Download archive
        run: |
          rclone copy gdrive:YouTubeArchive/archive.txt . || touch archive.txt

      # ⚡ RSS — VIDEOS
      - name: RSS Videos
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "duration > 60 & is_live = False" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Videos/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a feeds.txt

      # ⚡ RSS — SHORTS
      - name: RSS Shorts
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "duration <= 60 & is_live = False" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Shorts/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a feeds.txt

      # ⚡ RSS — LIVE
      - name: RSS Live
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "is_live = True" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Live/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a feeds.txt

      # 🧠 CANALES — VIDEOS
      - name: Channel Videos
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "duration > 60 & is_live = False" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Videos/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a canales.txt

      # 🧠 CANALES — SHORTS
      - name: Channel Shorts
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "duration <= 60 & is_live = False" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Shorts/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a canales.txt

      # 🧠 CANALES — LIVE
      - name: Channel Live
        run: |
          yt-dlp \
          --cookies cookies.txt \
          --match-filter "is_live = True" \
          --download-archive archive.txt \
          --no-overwrites \
          --merge-output-format mp4 \
          --embed-thumbnail \
          --add-metadata \
          -f "bestvideo[ext=mp4][vcodec^=avc1][height<=1080]+bestaudio[ext=m4a]/best[ext=mp4]" \
          -o "Live/%(uploader)s/%(upload_date>%Y-%m-%d)s - %(title).200s.%(ext)s" \
          -a canales.txt

      # 🔢 NUMERAR
      - name: Numerar videos
        run: |
          for tipo in Videos Shorts Live; do
              for dir in "$tipo"/*/ ; do
                  i=1
                  find "$dir" -maxdepth 1 -type f -print0 | sort -z | while IFS= read -r -d '' file; do
                      filename=$(basename "$file")
                      ext="${filename##*.}"
                      name="${filename%.*}"

                      name_no_date=$(echo "$name" | sed 's/^[0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\} - //')

                      new_name=$(printf "%s/%03d %s.%s" "$dir" "$i" "$name_no_date" "$ext")

                      mv -n "$file" "$new_name"

                      i=$((i+1))
                  done
              done
          done

      # ☁️ SUBIR
      - name: Upload videos
        run: |
          rclone move . gdrive:YouTubeArchive \
          --exclude ".git/**" \
          --exclude ".github/**" \
          --exclude "cookies.txt" \
          --exclude "canales.txt" \
          --exclude "feeds.txt" \
          --exclude "archive.txt" \
          --transfers 4 \
          --checkers 8 \
          --progress

      # 💾 HISTORIAL
      - name: Upload archive
        run: |
          rclone copy archive.txt gdrive:YouTubeArchive/

      # 🧹 LIMPIEZA
      - name: Cleanup
        run: |
          rm -rf */*
